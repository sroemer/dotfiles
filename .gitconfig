[user]
	name = Stefan Römer
	email = sroemer@github.com
[diff]
	tool = meld
[merge]
	tool = meld
[difftool]
	prompt = false
[mergetool]
	prompt = false
[difftool "neovim"]
    cmd = nvim -d $LOCAL $REMOTE
[mergetool "neovim"]
    cmd = nvim -d $LOCAL $MERGED $REMOTE
[alias]
    s = status
    a = add
    c = commit
    ca = commit --amend
    dotfiles = !git --git-dir=$HOME/Git/sroemer/dotfiles/ --work-tree=$HOME
    df = dotfiles
    lg = !"git log --oneline --graph --decorate --date=iso --all --format=format:'%x09%C(bold dim red)%h%C(reset) %C(green)%ad%C(reset) %C(normal)%s%C(reset) %C(dim white)(%an)%C(reset) %C(cyan)%d%C(reset)'"
[init]
	defaultBranch = main
